<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
    <div class="container">
        <!-- blog settings -->
        <h1 class="text-center"><%= edit.title %></h1>
        <h2 class="text-center"><%= edit.subtitle %></h1>
        <h3 class="text-center"><small><%= edit.author %></small></h1>
        
        <!-- all articles -->
        <h4 class="mt-3">Blog Articles</h1>
    
        <% articles.forEach(article => { %>
            <div class="card text-center mt-3">
                <div class="card-body">
                    <h4 class="card-title"><%= article.title %></h4>
                    <h5 class="card-subtitle mb-2 text-muted"><%= article.subtitle %></h5>
                    <p class="card-text"><%= article.published %></p>
                    <br>
                    <a href="/user/home/<%=article.id %>" class="btn btn-primary">Read More</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>