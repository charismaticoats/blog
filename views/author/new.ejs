<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
    <div class="container">
        <!-- blog settings -->
        <h1 class="text-center"><%= blog.title %></h1>
        <h2 class="text-center"><%= blog.subtitle %></h1>
        <h3 class="text-center"><small><%= blog.author %></small></h1>

        <!-- form to create new article -->
        <div class="card mt-3">
            <div class="card-body">
                <h4 class="card-title">New Article</h4>
                <form action="/author" method="POST">
                    <label for="title" class="card-text">Title</label>
                    <input id="title" type="text" name="title" placeholder="Title" class="form-control" required>
                
                    <label for="subtitle" class="card-text mt-1">Subtitle</label>
                    <input id="subtitle" type="text" name="subtitle" placeholder="Subtitle" class="form-control" required>
                
                    <label for="body" class="card-text mt-1">Body</label>
                    <input id="body" type="text" name="body" placeholder="Body" class="form-control" required>

                    <a href="/author/home" class="btn btn-secondary mt-3">Cancel</a>
                    <button type="submit" class="btn btn-primary mt-3">Save</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>