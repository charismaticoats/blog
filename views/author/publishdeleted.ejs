<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Blog</title>
</head>
<body>
    <div class="container">
        <!-- blog settings -->
        <h1 class="text-center"><%= blog.title %></h1>
        <h2 class="text-center"><%= blog.subtitle %></h1>
        <h3 class="text-center"><small><%= blog.author %></small></h1>

        <div class="card mt-3">
            <div class="card-body">
                <h4 class="card-title">Delete Article?</h4>

                <!-- article data -->
                <div class="card text-dark bg-light mb-3">
                    <div class="card-body">
                      <p class="card-text">Created: <%= article.created %></p>
                      <p class="card-text">Last Modified: <%= article.published %></p>
                      <p class="card-text">Status: Published</p>
                    </div>
                </div>

                <!-- form to show article content and to delete article -->
                <form method="POST">
                    <label for="title" class="card-text">Title</label> 
                    <input id="title" type="text" name="title" value="<%= article.title %>" class="form-control" required>
                    
                    <label for="subtitle" class="card-text mt-1">Subtitle</label>
                    <input id="subtitle" type="text" name="subtitle" value="<%= article.subtitle %>" class="form-control" required>
                    
                    <label for="body" class="card-text mt-1">Body</label>
                    <input id="body" type="text" name="body" value="<%= article.body %>" class="form-control" required>
                    
                    <a href="/author/home" class="btn btn-secondary mt-3">Cancel</a>
                    <button type="submit" class="btn btn-danger mt-3">Delete</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>